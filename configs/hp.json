{
    "electra" : { 
        "output_dir" : "../models/electra",
        "num_train_epochs": 3,
        "per_device_train_batch_size": 4,
        "per_device_eval_batch_size":4,
        "gradient_accumulation_steps":4,
        "learning_rate":2e-5,
        "weight_decay":0.01,
        "fp16":true,
        "gradient_checkpointing":true,
        "optim":"adamw_torch",
        "eval_strategy":"epoch",
        "save_strategy":"epoch",
        "load_best_model_at_end":true,
        "metric_for_best_model":"f1",
        "save_total_limit":2,
        "logging_steps":50,
        "logging_strategy":"steps",
        "seed":42
    },
    "mbert_dist" : { 
        "output_dir" : "../models/mbert_dist",
        "num_train_epochs": 3,
        "per_device_train_batch_size": 4,
        "per_device_eval_batch_size":4,
        "gradient_accumulation_steps":4,
        "learning_rate":2e-5,
        "weight_decay":0.01,
        "fp16":true,
        "gradient_checkpointing":true,
        "optim":"adamw_torch",
        "eval_strategy":"epoch",
        "save_strategy":"epoch",
        "load_best_model_at_end":true,
        "metric_for_best_model":"f1",
        "save_total_limit":2,
        "logging_steps":50,
        "logging_strategy":"steps",
        "seed":42
    },
"mdeberta": {
  "output_dir": "../models/mdeberta",

  "num_train_epochs": 1.5,

  "per_device_train_batch_size": 1,
  "per_device_eval_batch_size": 1,

  "gradient_accumulation_steps": 16,

  "learning_rate": 1.5e-5,
  "weight_decay": 0.01,

  "fp16": false,

  "gradient_checkpointing": true,

  "optim": "paged_adamw_8bit",

  "max_grad_norm": 1.0,

  "eval_strategy": "steps",
  "eval_steps": 2000,

  "save_strategy": "steps",
  "save_steps": 2000,

  "load_best_model_at_end": true,
  "metric_for_best_model": "f1",

  "save_total_limit": 2,

  "logging_steps": 100,
  "logging_strategy": "steps",
  "seed": 42,
  "dataloader_num_workers": 2
}
}